apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopslab9.fullname" . }}
  labels:
    {{- include "devopslab9.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopslab9.selectorLabels" . | nindent 4 }}
